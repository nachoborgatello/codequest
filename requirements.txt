# 📦 Dependencias principales
streamlit>=1.22.0       # Para la interfaz web
pytest>=7.2.0          # Para pruebas unitarias
python-dotenv>=0.21.0  # Para manejar variables de entorno (opcional)

# 🛠️ Dependencias de desarrollo (opcionales)
black>=22.10.0         # Formateador de código
flake8>=6.0.0          # Linter para Python
pytest-cov>=4.0.0      # Reporte de cobertura de tests

# 📊 Para análisis de datos (si amplías el proyecto)
pandas>=1.5.0         # Manejo de datos (opcional)
sqlalchemy>=2.0.0     # ORM para bases de datos (opcional)